{
  "name": "ahumada",
  "version": "0.0.3",
  "description": "Api pública del servicio de Buses Ahumada (Chile)",
  "license": "MIT",
  "repository": "jlobos/ahumada",
  "author": "Jesús Lobos <jlobitu@gmail.com>",
  "main": "lib/index.js",
  "scripts": {
    "build": "npm run clean && babel src -d lib",
    "clean": "rimraf lib/*",
    "prepublish": "npm run build",
    "test": "npm run build && ava && standard --verbose | snazzy"
  },
  "engines": {
    "node": ">=4"
  },
  "keywords": [
    "ahumada",
    "andes",
    "api",
    "buses",
    "felipe",
    "santiago"
  ],
  "dependencies": {
    "cheerio": "^0.22.0",
    "request": "^2.75.0"
  },
  "devDependencies": {
    "ava": "^0.16.0",
    "babel-cli": "^6.16.0",
    "babel-preset-es2015": "^6.16.0",
    "rimraf": "^2.5.3",
    "snazzy": "^5.0.0",
    "standard": "^8.6.0"
  }
}
